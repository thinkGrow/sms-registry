# AI Usage Log

I built this project with Claude Code as a collaborative pair programmer. Each entry documents a step: what I did, and how I used AI versus what I reviewed or decided myself.

### Setup
- I scaffolded the app with `create-next-app` (TypeScript, App Router, Tailwind, ESLint, `src/` dir, `@/*` import alias), using commands Claude suggested and running them myself in the terminal.
- The scaffold generated `AGENTS.md`/`CLAUDE.md` files flagging that Next.js 16 has breaking changes versus what's in Claude's training data. I agreed with Claude that it should check `node_modules/next/dist/docs/` before writing framework-specific code, instead of assuming older App Router conventions still apply.
- I asked Claude to keep commit messages structured (Conventional Commits: `feat`, `fix`, `chore`, `docs`, `test`, `ci`) for the rest of the project, and to log that instruction here.

### Design system
- I ran `shadcn@latest init` interactively in my own terminal, landing on the `base-sera` style with the `taupe` base color.
- Before adding feature components, I asked Claude to confirm a centralized color system and reusable components already existed. Claude verified shadcn's CSS variable token system and the existing `cva` based `Button` component, then extended `globals.css` with app specific semantic tokens (`success`, `warning`, `info`) for status flags like enrolment status, overdue fees, late submissions, and grade classification. These are defined once, for light and dark, rather than as one off colors per page.

### Database setup
- I added `docker-compose.yml` for local PostgreSQL, choosing that over a hosted database so anyone cloning the repo can run it with zero account signup, and the same image can later back a CI service container.
- I ran `npx prisma init`, which generated a placeholder `DATABASE_URL` pointing at Prisma's own hosted `prisma+postgres://` dev service instead of a plain Postgres connection string. Claude caught this and replaced it with the actual Docker Postgres connection string, and added the missing `dotenv` dependency that `prisma.config.ts` requires.
- I filled in the README's "Getting Started" and "Environment Variables" sections with the real setup steps once they existed, replacing the earlier placeholders.

### Schema design: Student and Programme
- Claude and I discussed the field by field reasoning for `Student` (a separate `id` versus a human facing `studentId`, `@db.Date` for date of birth, an enum for status, audit timestamps) and `Programme` (`Decimal` for money instead of `Float`) before writing the schema.
- I raised that students may get discounts through multiple programmes, financial aid, or scholarships. Claude recommended against building full subsystems for these, since they're out of spec scope and would crowd out the four graded modules given the time I have left. Instead we agreed on a single optional `feeOverride` field on Student, with the one programme per student simplification and its trade offs documented explicitly in the README rather than silently ignored. I decided the fuller logic can be layered in later once the core project is complete.
- We discussed why `academicYear` has to stay a plain editable field rather than being derived from enrolment date, because a retake can put a student in the same academic year twice. We also agreed enrolment `status` should gate assessment submission eligibility.
- I settled on Zod as the project wide validation convention, reused between forms and API routes, after asking Claude why client side validation alone isn't sufficient (a public API route can be hit directly, bypassing the form).
- I asked Claude to explain how to visualize the database design. We went with a hand maintained Mermaid ER diagram in the README instead of an auto generating tool like prisma-erd-generator, since it needs no extra dependency and GitHub renders it natively.

### Schema design: Payment and fee due dates
- I asked to discuss the Payment model entirely from scratch rather than refining an earlier draft. Claude restated the four open questions plainly: whether payments are installment style, where the reference number comes from, what defines an overdue balance, and whether a recorded payment can be edited.
- I confirmed payments are installment style, multiple rows accumulate toward a student's fee.
- I asked whether the reference number could be auto-generated instead of staff-entered. Claude agreed this was the better call since the app doesn't integrate with a real payment gateway or bank, so there's no external reference to capture, and generating it the same way as the Student ID avoids relying on staff data entry for uniqueness.
- I ruled out a "days since enrolment" definition of overdue, since a fee amount doesn't change based on what day someone signed up, and rejected "balance greater than zero" as too blunt since it would flag a student overdue on day one before any real deadline passed. I settled on a per-programme due date instead.
- I confirmed payments should be ordinary CRUD rather than a full accounting ledger with reversal entries, and asked Claude to document that as a deliberate scope exclusion in the README rather than leaving it unstated.

### Schema design: Assessment and Submission
- I asked Claude to explain the difference between a programme and a module, since the spec lists both but only defines Programme as its own entity. Claude clarified that Programme is the whole degree while module is one subject taught within it, and proposed keeping module as a plain text field on Assessment while adding a programmeId relation purely for submission eligibility.
- I pushed back that a student in Computer Science might not be registered for every module under it, exposing a real gap in programme-level eligibility scoping. I then raised that modules can also have prerequisites between each other.
- Claude named this directly: each concern I raised was individually valid, but stacking module registration and prerequisites on top of the earlier fee discounting discussion would turn one module of four into the entire remaining project. I agreed to keep programme-level scoping on Assessment and document the module-registration and prerequisite gap as a known limitation instead of building it.
- We finalized Assessment (title, module as plain text, programmeId, a full datetime deadline rather than a date, since lateness depends on an exact cutoff) and Submission (fileUrl, fileName, an enum fileType restricted to PDF or DOCX, a unique constraint on studentId and assessmentId so resubmission overwrites the existing row instead of keeping history, and isLate computed from updatedAt versus the assessment deadline rather than stored).

### Schema design: Grade
- I confirmed grades should allow half points, so I used Decimal instead of Int for score, matching the same reasoning as money fields, since the classification thresholds need exact comparisons rather than floating point.
- Claude pointed out the spec never names what a score under 40 is called, and I agreed treating it as an implicit Fail, computed at read time rather than stored, was the obvious resolution.
- Claude flagged a real ambiguity in the spec's wording, "publish or withhold results per student" could mean one switch for a student's entire marksheet, or a separate publish flag per individual grade. I confirmed per grade makes more sense, since staff would realistically publish results assessment by assessment as grading finishes rather than all at once.

### Seed script and a Prisma version surprise
- Claude wrote prisma/seed.ts covering every edge case we designed for in one pass: overdue balances, a fee override, all four enrolment statuses, a late submission, and a withheld grade, rather than generic placeholder rows.
- Rather than assume the old package.json convention for configuring the seed command, Claude checked the installed Prisma version's actual type definitions and found the seed command now lives under migrations.seed in prisma.config.ts instead.
- The seed script failed twice in a row on things Claude's training data wouldn't have known about in this Prisma version: the generated client has no index file, so it has to be imported from client.ts directly, and this generator requires an explicit Postgres driver adapter (@prisma/adapter-pg) rather than connecting automatically the way older Prisma versions did. Claude read the generated client's own source to confirm this instead of guessing, then set up a shared prisma client singleton at src/lib/prisma.ts using the adapter, which the seed script now reuses instead of instantiating its own client.

### Student Enrolment module
- I confirmed no hard delete for students, WITHDRAWN status covers a student leaving instead, so Claude only built create, read, and update endpoints.
- Before writing the dynamic route for a single student, Claude checked Next.js's own bundled documentation rather than assume the params shape from training data, since the scaffold's AGENTS.md file had already warned this Next.js version has breaking changes. It confirmed params is a Promise that needs awaiting, which is what got built.
- Claude wrote the Zod validation schemas, the sequential student id generator, and both API route files, then started the dev server itself and ran curl requests against every endpoint (list, search, status filter, create, single fetch, update, duplicate email, invalid email) to verify each one before telling me it was done, rather than just asserting the code should work. It found the newly created test student got an SMS-2026 id while the seed data uses SMS-2025, since the generator uses the real current year rather than the spec's illustrative example year, and explained that wasn't a bug. It then re-ran the seed script to remove the test record it had created during verification, restoring the clean seeded dataset.
- I said going forward I want to write Zod schemas myself, with Claude giving instructions instead of writing that file directly.

### Student Enrolment UI
- Claude added the select and dialog shadcn components. The form component didn't install, view form on the registry returned an empty entry, which Claude attributed to this project using Base UI instead of Radix as its primitive library, since the shadcn form wrapper may not be ported to Base UI yet. Rather than fight that, it built the form directly with React Hook Form against the existing Input, Label, and Select components instead of the shadcn form wrapper.
- Claude built the students page, a filter bar, a table, and a create and edit form dialog, then started the dev server and curled the page rather than assuming it worked. That surfaced two real bugs: Prisma's Decimal type can't cross the Server to Client Component boundary as is, so Claude added a serialize.ts helper converting Decimal fields to plain numbers before passing data to client components, and the Dialog trigger used a Radix style asChild prop that doesn't exist on this Base UI based component, which Claude caught by reading the actual dialog.tsx source rather than assuming, and fixed using this component's own render prop pattern instead.
- Claude also caught and rewrote its own first draft of the Select field wiring, which manually dispatched a fake onChange event into react-hook-form's register function. That was fragile, so it replaced it with react-hook-form's Controller component, the standard way to connect a non native form control to react-hook-form.
- After both fixes, Claude re-verified server side rendering was clean before handing it back for me to click through in an actual browser myself, since it doesn't have browser automation tools to test the interactive dialog and form submission directly.
- I found a real bug through actual browser use that curl testing couldn't have caught: the programme filter dropdown displayed the raw database id instead of the programme name. Claude traced this to Base UI's Select.Value component, which needs an explicit children render function mapping the selected value to a label, unlike Radix which does this automatically by matching against the rendered SelectItem. It fixed all four Select instances across the filter bar and the form dialog, then verified the fix by checking the actual rendered HTML output for the programme name rather than just asserting the code change should work.
- I reported date of birth wasn't saving. Claude first tested the API directly with curl and confirmed it persists date changes correctly, narrowing the bug to the form itself. It then identified a real React Hook Form gotcha: useForm's defaultValues are only computed once, when a component first mounts, and since the same edit dialog instance for a table row isn't destroyed and recreated just because its student prop's data changed after a save, reopening Edit on that row was showing the stale values captured at first mount rather than the freshly saved record. It also noted the table doesn't display date of birth anywhere, so reopening Edit was the only way to check it, which is exactly the scenario that triggers this bug, supporting that diagnosis. The fix keys each row's dialog on the student's updatedAt timestamp, forcing a fresh mount whenever the record actually changes.
- I asked where to see a student's full info, since the table only shows a summary. Claude added a read-only student detail page at /students/[id], linked from the table, reusing the same edit dialog from there too. It verified the Button component's render prop (used to compose it with a Next.js Link) actually exists on this Base UI component before using it, by checking the library's own type definitions rather than assuming, and caught a repeat of the earlier Decimal serialization bug on this new page (the student's nested programme relation wasn't re-serialized before being passed to the client-side form dialog), fixing it before handing it back.
- I reported date of birth showed correctly on the detail page but not in the edit form itself. Claude traced this by reading Base UI's actual Input implementation (it turned out to be a wrapper around their Field.Control component, with its own internal ref and value handling separate from a plain native input), but couldn't fully verify the exact cause without live browser testing. Rather than keep guessing, it converted the date field to a fully controlled input via react-hook-form's Controller, the same pattern already used successfully for the two Select fields, removing the dependency on uncontrolled defaultValue behavior entirely regardless of the precise internal cause.

### Fees & Payments
- Claude wrote a shared balance.ts computing outstanding balance and overdue status from a student's fee override or programme fee minus their payments, computed at read time rather than stored, and a payment reference generator following the same pattern as the student id one.
- I wrote the first draft of the Payment Zod schema myself but left the Student fields copied in as a placeholder; Claude swapped in the actual Payment fields (studentId, amount, paidAt). I then asked Claude to add the update schema directly rather than writing it myself this time, omitting studentId since reassigning a payment to a different student isn't something we want to support.
- Claude built the create, update, and delete payment API routes, then verified all of them with curl (valid creation with correct sequential reference number, rejected negative amount, rejected nonexistent student, update, delete) before moving on, rather than assuming they worked.
- Claude built the payment history section on the student detail page, overdue badges on the student list, and a Registry dashboard at / listing overdue students by name. Testing it surfaced yet another instance of the Decimal-across-client-boundary bug, this time via the newly added payments include on the detail page leaking through serializeStudent's blanket object spread. Rather than patch this one instance, Claude rewrote serializeStudent and serializeProgramme to explicitly pick fields instead of spreading, so a future query's include can't cause the same class of bug again. It then verified the dashboard by checking it named exactly the three students expected to be overdue from the seed data.

### Assessment Submission
- I asked Claude to build this whole module itself (schemas included) rather than writing the Zod schemas myself this time, given its complexity, so we adjusted our earlier workflow note to reflect that as a case by case choice rather than an absolute rule.
- Claude wrote a submission upload route handling multipart file upload, MIME type validation (PDF or DOCX only), programme and enrolment-status eligibility checks, and the resubmission-only-before-deadline rule from our schema design discussion. It verified all six branches with curl before writing any UI: valid upload, invalid file type, wrong programme, withdrawn student, blocked resubmission after a deadline, and a first-time late submission being accepted rather than blocked, temporarily creating a throwaway test student for the last case and cleaning up afterward by re-seeding.
- Building the assessment detail page surfaced two more instances of the same Decimal-across-client-boundary bug, this time from passing whole raw Prisma objects (the assessment with its nested programme and submissions relations, and the eligible-students list with feeOverride) to components that only needed a few plain fields. Claude fixed both by passing explicitly constructed plain objects instead of the raw query results.
- Testing the late-submission badge surfaced a real seed data bug: only Brian's Data Structures submission had its timestamp explicitly backdated, so every other submission defaulted to the actual seed run time, which was after every past assessment deadline, making them all appear late instead of just the intended example. Claude rewrote the seed script to explicitly backdate every submission to its intended illustrative date, then re-verified Algorithms Midterm showed zero late badges and Data Structures Assignment 1 showed exactly one, for Brian.

### Marksheet & Results
- Claude wrote the classification helper and grade schema itself, keeping the momentum from taking over the whole Assessment Submission module, and used Prisma's upsert so entering a grade for an already-graded student overwrites the score instead of erroring on the unique constraint.
- It verified the grade API directly with curl first: upsert overwriting an existing score, out-of-range scores rejected both above 100 and below 0, and publish/withhold correctly setting and clearing publishedAt.
- The Grades section on the assessment page and the Marksheet section on the student page were both built carefully to only pass explicitly constructed plain objects to client components, applying the lesson from the three earlier Decimal-boundary bugs upfront instead of hitting a fourth one. Testing confirmed a clean dev log and the exact expected data (Alice's Distinction/Published, Brian's Merit/Published, Grace's Fail/Withheld) on the first try.

### Staff/Student role separation
- Claude checked Next.js's own bundled docs for the current Server Actions and cookies() conventions before writing the session code, rather than assume from training data, matching the pattern established earlier for route handlers and page params in this Next.js version.
- Rather than build a whole separate set of student-facing pages, Claude reused the existing staff pages with conditional rendering based on the session's role, since most of the underlying data and logic were already correct, only visibility and a few access rules needed to change. This closed out several interim decisions flagged earlier: the student picker in the submission form and the "shows everything, will be gated later" note on the marksheet both got their real gating now.
- Since Server Actions can't be triggered directly with curl, Claude verified the access control logic by setting the same session cookie manually and testing every page: dashboard and student list redirect a student to their own profile, viewing another student's profile redirects back to their own, the assessments list filters to their own programme with no create button, an assessment from a different programme shows a blocked message instead of a submission form, and the submissions table and grades section correctly restrict to their own data. One check initially looked like a bug (other students' names appeared in the raw HTML of an assessment page) until Claude traced it to the eligibleStudents prop still being embedded in the page's hydration payload despite not being visually rendered, then confirmed with a more precise check (no links to other students' profiles) that it was a false alarm, not an actual leak.

### Layout width and dev server console findings
- I asked to widen the dashboard, then to make every page consistently wide rather than each having its own container width. Claude unified all five pages plus the nav bar to the same max-w-7xl.
- When I said nothing had visibly changed even after a hard refresh, Claude checked the actual HTML and CSS the live server was returning rather than guessing at browser causes, confirming the class and its underlying CSS rule were both correctly present, which narrowed the problem to something browser-side rather than a build issue.
- While troubleshooting that, I accidentally had my own dev server killed by one of Claude's cleanup commands (pkill matched by process name, not by which terminal started it) - Claude flagged this immediately and I restarted it myself.
- I later pasted a large chunk of terminal output from my own testing session. Claude read through it and found two real bugs neither of us had been specifically looking for: a Base UI warning that Button needs nativeButton={false} whenever it's composed with a Link instead of a real button element (fixed in all three places this pattern was used), and a 404 on a seeded submission's file link, since the seed script only ever created database rows referencing upload paths, never the actual files on disk. Claude fixed the seed script to write a real placeholder file for each seeded submission and confirmed the previously-404ing link now returns 200.
- The width issue itself took a long back and forth: Claude confirmed via curl that the HTML, the compiled CSS rule, and the CSS variable it referenced were all correct on the server side, ruling out a build problem, then asked me to check Chrome DevTools directly rather than keep guessing blind. I suspected shadcn's Card component; Claude checked its source and found no width constraints there either. The DevTools screenshot I sent showed the actual culprit: a header div inside the page was shrink-wrapped to its content (423x40) instead of stretching full width. Claude traced this to body being a flex container (flex flex-col in the root layout) that wasn't needed for anything, and removed it, converting body back to a plain block container where width behavior is unambiguous. That fixed it.

### Mobile responsiveness and seed data expansion
- I asked for the app to be mobile responsive. Claude restructured the nav to stack on small screens, made page headers and the payments summary row stack instead of overflowing, and checked (rather than assumed) that shadcn's Table component already wraps itself in a horizontal-scroll container, so no table changes were actually needed.
- I asked for a smaller, separate line for student IDs in the nav's student picker and a smoother dropdown transition. Claude found the transition was only 100ms and bumped it to 200ms with an easing curve, a change that applies to every Select in the app since it lives in the shared component.
- I asked for the seed data to cover way more scenarios. Claude added a third programme with no fee due date specifically to demonstrate that a programme without one never flags students overdue regardless of balance, seven more students spanning every status again, an installment-payment example (two payments building toward one fee), a fifth assessment left with zero submissions to show that empty state, and additional grades specifically to fill in a Pass classification that had been missing from every previous version of the seed data. It verified the null-due-date exclusion and the empty-submissions state by checking exact profile links and counts rather than raw text matches, since two earlier checks in this same session had already turned out to be false positives from the Nav's hydration payload containing every student's name on every page.
- I asked that students be able to view another student directly from student view, not just switch back to staff first. Claude added the student picker to both roles, filtering the currently-viewed student out of their own options list since switching to yourself doesn't make sense.
- I asked for students to be able to pay their own fees online (a dummy flow, no real payment gateway, no need to show their balance first), with staff keeping their own manual entry option too. Claude built a separate, simpler dialog for this (just an amount, no reference number or date fields since it's paid immediately) reusing the same payment API and reference-number generation as the staff flow, moved its Zod schema into the existing validations file for consistency with the rest of the codebase, and verified both the role-based visibility (staff sees "Record Payment", students see "Make a Payment", never both) and the actual payment creation end to end before documenting it as a deliberate scope decision in the README.
- I asked whether we'd actually implemented publish or withhold, and whether students only see published results. Claude checked the actual API routes rather than just the UI and found neither /api/grades route had any server-side role check at all, the publish/withhold controls were only ever hidden by the UI, not genuinely protected, and that this was true across every API route in the app. I confirmed the fix should come after this round of feature requests rather than immediately.
- I then asked why staff even had an option to submit an assignment in the first place, since that seemed odd. Claude was honest that this wasn't a deliberate decision, it was a leftover student-picker stand-in from before role separation existed that never got revisited, laid out a real use case for keeping it (staff uploading on behalf of a student with technical trouble) against removing it entirely, and I chose to remove it. Claude then simplified the SubmissionForm component itself to drop the now-dead student-picker branch entirely rather than just hiding it behind a flag, and replaced a query that fetched an entire programme's roster with a single targeted eligibility check, since the roster was only ever being fetched to check one student's membership in it.

### Installment-based fee billing
- I asked how fees actually work realistically, since nobody pays a whole programme fee at once and the model didn't account for a student progressing to their next year at all. Claude explained the existing model honestly (one flat total for the whole programme, never tied to academic year) and laid out two options: document it as a known limitation, or model fees per academic year as a real (and substantially bigger) feature.
- I proposed installments as x/4 for Bachelor's and x/2 for Master's. Claude flagged the part that determines whether an installment is actually overdue, how each installment's due date gets determined, evenly spaced from one shared date versus a real per-installment schedule, and recommended the simpler evenly-spaced approach given time left.
- I clarified the spacing should be one installment per year, tied to programme length (4 years for Bachelor's, 2 for Master's), which directly answered the earlier "what happens on year progression" question, since cumulative fee owed now scales with academic year. I then pointed out a real gap in that plan: someone who enrolled back in 2025 should be able to show as further behind than someone enrolling now, which the programme-wide due date couldn't represent. Claude added enrolmentDate as a new per-student field specifically so each student's installment schedule is anchored to when they personally started, not a shared date, and confirmed the plan before implementing rather than assuming.
- Claude added degreeLevel to Programme and enrolmentDate to Student (both required fields), rewrote the balance calculation to compute installments due from elapsed time since each student's own enrolment date, updated the seed data with individually varied enrolment dates so the demo would actually exercise the new logic (not just add the field), and updated the student form, student detail page, and payments UI to expose the new field and the installment breakdown.
- Since both new fields were required on tables that already had rows, a normal migration needed an interactive backfill prompt Claude couldn't answer non-interactively. Prisma itself detected the AI-invoked `migrate reset` command and refused to run it without explicit human confirmation, correctly identifying it as irreversibly destructive; Claude explained exactly what the command was, why it was needed, and what it would destroy, and I ran it myself in my own terminal rather than have Claude run it directly.
- After reseeding, the overdue count came back as 0 when Claude had predicted 5. Rather than assume the new logic was wrong, it checked the actual API response first and found enrolmentDate and degreeLevel were missing entirely, tracing this to our own long-running dev server caching a single Prisma Client instance across hot-reloads (a pattern we'd deliberately built for connection-pool reasons), meaning it hadn't picked up the regenerated client after the schema change. A server restart fixed it, and the overdue count then matched the hand-calculated prediction exactly, including the specific mechanism this whole feature was built to demonstrate: Brian's exact computed values (balance 3000, 2 of 4 installments due, 500 owed by now) matched by hand before checking the live data.

### Real server-side authorization
- I agreed to fix the API authorization gap flagged earlier. Claude wrote one shared helper (requireStaff, requireStaffOrSelf, requireSelf) rather than repeating the same logic per route, and applied it consistently: staff-only for creating or editing students and assessments and for entering or publishing grades, staff-or-self for recording a payment, and self-only for submitting assessment work, matching the earlier decision that staff can no longer submit on a student's behalf even via a direct API call.
- Claude verified every one of these with curl rather than trusting the code read: staff succeeding and a student being rejected on publish/withhold (the exact case that started this), a student paying for themselves succeeding while paying for another student was rejected, and a student being blocked both from spoofing another student's id on a submission and from submitting at all as staff. All checks passed on the first try. It then cleaned up the test grade and payment created during this verification by re-seeding, rather than leaving them in the demo dataset.
- I asked why a student's balance was showing up even on their own page, since they shouldn't know it. Claude checked the payments section component directly and found the whole Fee/Paid/Balance/installment/overdue summary line was rendered unconditionally, with no role check at all, contradicting the reasoning already established for the online payment dialog. It gated that summary behind the same canManage flag already used elsewhere on the page, keeping the payment history table and the "Make a Payment" button visible to the student, and verified with curl that staff still see the balance while a student viewing their own page does not.
- I then also asked Claude to rename the "Balance" label everywhere, since it read like we were claiming to know a student's actual bank account balance rather than what they owe the institution. Claude renamed it to "Outstanding Fees" across the payments section and the dashboard.
- I pushed back on hiding the fee summary from students at all, since a student obviously needs to know what they owe to make sense of paying anything, "Make a Payment" with zero context isn't usable. Claude agreed this had over-extended the original ask (which was specifically about the payment dialog itself not needing to check balance before accepting an amount, not a blanket information ban) and reverted the gating, restoring the same fee summary for both roles, keeping the actual role difference scoped to which action buttons appear rather than what information is shown. It also caught its own false alarm mid-verification: an "Installment" text search initially came back empty, which turned out to be React inserting hydration comment markers between adjacent text and expression nodes in the SSR output, not a real rendering failure, confirmed by refetching cleanly rather than assuming the fix had broken something.
- I asked why I couldn't edit an assessment. Claude tested the underlying API directly and confirmed it worked, then walked through the likely role-session mix-up from before, until I realized the actual issue myself: the students list has an inline Edit button right in the table, but the assessments list only had a plain title link with no button, much less discoverable, so I'd never realized clicking through to the detail page was where editing lived. Claude added a "Details" button to both list tables for consistency, and in verifying it, noticed the assessments API returned one more assessment than the seed data creates, a "Sorting" assessment I'd created myself while testing the New Assessment form, which I confirmed I wanted to keep rather than have cleaned up.
- I asked for payments to be capped once a student's outstanding fee reaches zero, no further payments accepted. Claude added one shared helper (getStudentFeeAmount) rather than duplicating the fee-override logic, and enforced the cap in both the create and edit payment routes, so staff can't bypass it by editing an existing payment's amount upward either. While verifying with curl, one test's expected numbers didn't match reality, a student I assumed had zero payments recorded actually had $1,000 already logged; rather than treat this as a bug, Claude recognized it as likely a payment I'd made myself while testing the app directly (same pattern as the "Sorting" assessment), adjusted the test to the real numbers, and confirmed all six scenarios (already fully paid, an overshoot, the exact remaining amount, fully paid afterward, an edit that would exceed the cap, and a valid edit) behaved correctly before cleaning up the specific payment it had created for testing and confirming the student's original balance was restored.
- I said assessments felt disorganized, not grouped by module. Claude changed the query's sort to module-then-deadline and grouped the flat list into one Card per module (dropping the now-redundant Module column since it's implied by the section heading), verified the counts per module matched the database exactly for both a staff view (all 5 modules) and a student view (only their own programme's 2 modules).
- I settled the deferred-student billing question left open earlier: for simplicity, assume a flat one year per deferral rather than tracking real deferral dates, so a Bachelor's student who defers once ends up on a 5-year schedule instead of 4. This needed a schema change, so Claude added Student.deferredYears (Int, default 0) and ran the migration directly, since it was additive with a default and didn't need the interactive backfill that blocked the earlier enrolmentDate/degreeLevel migration. It subtracted deferredYears from yearsElapsed in calculateStudentBalance (clamped at zero) so the offset permanently shifts the schedule rather than just pausing it, and auto-increments the field in the students API route only when status actually transitions into DEFERRED from something else, not on every edit to an already-deferred student. Hit the exact same stale-Prisma-Client symptom as before (NaN in the installment count) until I restarted the dev server myself, after which the numbers matched: David Lee's schedule dropped from 3 of 4 installments due to 2 of 4. Rather than reseed the whole database (which would have wiped out real testing data I'd built up in the browser, a payment and a regraded score included), Claude updated David's deferredYears directly via a single UPDATE, and updated the seed script's comment and value together so future reseeds stay consistent with what's actually in the database now.
- I sent a screenshot showing David Lee, viewing as himself, blocked from submitting to Business Ethics Essay with the message "this assessment isn't open to your programme," even though Business Ethics is exactly his programme (BA Business Administration). Claude recognized this connected directly to the deferred-status question left open earlier: David is deferred, not enrolled, and the eligibility check had conflated "wrong programme" and "not enrolled" into a single check with one generic message, so the actual reason (his status) was getting reported as the wrong one (his programme). It split the check into two independent conditions with their own messages, verified David now sees the correct "you need to be actively enrolled" message, and confirmed a genuinely wrong-programme student (Grace, Computer Science, viewing a Business Administration assessment) still sees the original programme message correctly.
- I then pointed out the assessment's deadline was still open, which read as a question of whether a deferred student should be able to submit as long as the window hasn't closed yet, since deferred is a temporary pause rather than a permanent exit. Claude proposed loosening the rule to let deferred students submit while withdrawn/completed stayed blocked, and started implementing it, until I corrected that deferred should still block submission entirely regardless of the deadline, being deferred means not currently doing coursework at all. Claude reverted to the original enrolled-only rule but kept the actual improvement: each blocked status (deferred, withdrawn, completed) now gets its own specific message naming the real reason, instead of one generic "you need to be enrolled," in both the API route's 403 response and the page's UI message.
- I asked for the nav bar to show who's currently logged in, name and enrolment status for a student, or "Staff," since there wasn't previously any always visible indicator of whose view you were in. Claude added the current student's status to the existing student list query in the root layout (it was already being fetched for the switch-to-student picker, just missing the status field) and reused the same shared enrolment-status label/badge-variant map already used on the student list table, rather than inventing new status styling, then verified with curl as both a deferred student and staff that the correct name/badge or "Staff" badge renders in each case.
- I asked whether resubmitting is blocked once a submission has already been graded. Claude tested this directly rather than guessing (curl resubmission against a real submission of mine, Alice's Sorting assignment, which already had a published grade), confirmed the app allows it, deadline is the only thing that blocks resubmission, and disclosed as a side effect that the test had overwritten the actual file I'd uploaded for that submission (resubmission deletes the old file on disk), since that was my real test data, not something to silently alter.
- I then pointed out staff has no way of knowing a resubmission happened at all, since it silently overwrites the same row with no visible trace. Claude added two computed badges to the staff submissions table on the assessment detail page: "Resubmitted" whenever a submission's createdAt and updatedAt differ, and a more specific "Resubmitted after grading" instead whenever the resubmission happened after the grade was last saved, the case that actually matters since it means the existing score may not reflect the newest file. Both are computed at read time from existing timestamps, no new fields, and verified against Alice's Sorting submission (correctly flagged) against a never-resubmitted submission on a different assessment (correctly showed neither badge).
- I asked for a resubmit message on the student side too. Claude added a note above the submit form, shown only when the student already has a submission, naming the actual file and date on record and warning it'll be replaced, with an extra line about the grade possibly going stale if one already exists, plus a success confirmation in the form itself after a submission or resubmission goes through ("Submitted." versus "Resubmitted. This replaced your previous file.", based on whether one existed beforehand). Verified the note appears correctly for Alice's already-graded Sorting submission (showing her actual current file name, confirming it reflected the resubmission she'd done herself in the browser after Claude's earlier test overwrote it) and stays absent for a student submitting for the first time.
- I asked for students to be able to see their grade or pending status directly on the assessments list and single assessment pages, not just buried in their profile's Marksheet. Claude scoped the Prisma query on the list page to fetch only the current student's own submission/grade (rather than the whole cohort, which staff still get for the existing "Submissions" count column), reused the same classification labels/badge variants already used on the Marksheet, and added a "Your Result" column showing "Not submitted," "Pending" (covers both not-yet-graded and graded-but-withheld, since a student can't tell those apart anyway and shouldn't need to), or the published score and classification. Added the same result column to the submission table on the single assessment page. Verified with curl across three real students: Alice (three different published classifications, including one, Sorting, she'd been regrading herself in the browser during this session), Carla (a genuinely pending, submitted-but-ungraded case), and staff (confirmed unaffected, still showing the cohort submission count instead).
- I asked that switching identity while on an assessment page keep me on that same page, rather than always bouncing to a profile. Claude had both switchToStaff and switchToStudent server actions always redirect to a fixed destination regardless of where the switch was triggered from. It added a shared preservedDestination helper, passed the nav's already-available usePathname() value into both actions, and kept the fallback (own profile for a student, dashboard for staff) for every other page, since dashboard, the students list, and another student's profile all have genuinely role-specific content that isn't meaningful to preserve across a switch. Server Actions aren't curl-testable the way API routes are (no plain REST endpoint, Next invokes them through its own action-id protocol), so this one was verified by reading the logic through rather than an automated check, left for me to confirm directly in the browser.
- I asked for staff to have a clear view of ungraded assignments. Claude added this to the dashboard rather than the assessments list, since it's the natural staff landing page and already follows exactly this pattern for overdue fees: a headline stat card plus a list below it. Since Submission and Grade have no direct database relation to each other (both key independently off studentId/assessmentId), it fetched both tables and computed the ungraded set in application code, the same technique already used to match a submission to its grade on the assessment detail page. It initially colored the ungraded count using the warning token directly as text color, then caught its own mistake before I had to: warning is a light/bright token meant to pair with its own foreground color on a filled badge background, not to stand alone as text, unlike destructive (used for the overdue count) which is dark enough to read fine on its own. Reverted that one to plain text. Verified the list against real data, four ungraded submissions across the whole system, sorted oldest-first, count matching the list length exactly, and confirmed the page still redirects students away entirely as before.
- I asked whether the ungraded indicator should also show up on the assessments pages themselves, not just the dashboard. Claude added the same computed definition in three more places: an "N ungraded" badge per row on the assessments list (reusing the submissions/grades already being fetched there for the student-facing result column, just computed the other direction for staff), an "Ungraded" badge per row on the detail page's submissions table alongside the existing Late/Resubmitted badges, and a count in the Grades section's own title. Claude initially wrote the title count inline as an IIFE inside the JSX, caught that this was messy on its own, and pulled it out into a plain ungradedCount variable next to the existing gradeRows computation instead. Verified against Business Ethics Essay (2 submissions, both ungraded, matching on the list badge, the detail page badges, and the Grades title) against Sorting (fully graded, correctly showing no ungraded indicator anywhere).
- I sent a screenshot pointing out the Grades table has no way to actually review what's being graded, just a student name and a bare score input, no file. Claude added fileUrl/fileName to the existing gradeRows mapping on the assessment detail page (it was already building this array from each submission, just hadn't carried those two fields through) and added a File column to the Grades table linking to the same upload, reusing the exact link markup already used in the Submissions table above it. Verified against Data Structures Assignment 1, both Brian's and Liam's file links render and point at the correct uploaded files.
- I asked for a confirmation message when saving a grade, since the Save button previously gave no feedback at all beyond the row re-rendering with the new score already in the table (easy to miss, especially if the value hadn't visibly changed). Claude added a per-student savedFor state, shown as a small "Saved" label next to the Save button, cleared the moment that row's score input is edited again so it can never read as confirming a value that hasn't actually been saved yet. This one is a client-side interaction (fetch call plus local React state), not reachable through curl the way a plain API route is, so it was verified by reading the logic rather than an automated check.
- I asked for a programme page showing fees broken down year-wise with the per-year value. There was no programme list or detail page in the app at all before this, programmes only ever existed as options inside student/assessment forms. Claude added a getYearlyFeeBreakdown helper next to the existing calculateStudentBalance in balance.ts (exporting the previously-private TOTAL_INSTALLMENTS map alongside it), computing the same even per-year split a student's own balance uses, but at the programme level rather than anchored to any individual student's enrolment date or fee override. Built a /programmes list (name, degree level, total fee, per-year amount, enrolled count) and a /programmes/[id] detail page (the same info plus a full year-by-year table with a running cumulative total), and added a Programmes link to the nav for both roles, since a programme's fee schedule isn't private student data the way an individual's balance is. Verified the math directly against seed data: MSc Data Science, $6,000 total split across 2 years, correctly showing $3,000 then $3,000 with cumulative totals of $3,000 and $6,000, and confirmed the "N enrolled" link correctly filters the students page to that programme.
- I immediately pointed out students shouldn't see the enrolled-student count on the new programme pages. Claude gated both the list column and the detail page's count/link behind isStaff, the same reasoning already applied to the assessments list's cohort-wide "Submissions" count, and verified with curl that a student sees neither the column nor the link while staff still see both.
- I asked where CRUD for programmes was, there wasn't any, programmes only ever existed via seed data, which the README already called out as a deliberate read-only scope decision. I asked Claude to build it now, following the same pattern Students and Assessments already use (Dialog + react-hook-form + Zod, staff-only via requireStaff, no delete). Normally I write the Zod schema myself and Claude works from a spec; this time I told it to just write the schema too, so it wrote src/lib/validations/programme.ts (name, feeAmount, degreeLevel enum, nullable feeDueDate) matching the exact conventions already in student.ts and assessment.ts, then built /api/programmes and /api/programmes/[id] (GET/POST/PATCH, no DELETE), a ProgrammeFormDialog reusing SerializedProgramme, and wired New Programme / per-row Edit buttons into both programme pages, staff-only. I also asked whether degreeLevel was actually driving the x/4 vs x/2 installment split; Claude confirmed it's the single field TOTAL_INSTALLMENTS keys off, so the form needs no separate installment calculation of its own. Verified with curl: create, edit, a rejected duplicate name (409), and a student blocked from creating a programme (403), then cleaned up the test programme it had created.
- I asked for payments to stop being free-form and instead be picked per installment year (Year 1 through the programme's total installments), listed that way in the payments table. This needed a schema change (Payment.installmentYear, nullable since existing payments predate the rule and don't cleanly map to one year each), which I wrote myself again given the scope, and Claude ran the additive migration directly. It moved amount computation entirely server-side (feeAmount / totalInstallments, never client-supplied), rejected an already-paid year or a year beyond the programme's length, and rebuilt both payment dialogs (staff and the student's own online payment) around a year Select that excludes years already paid, falling back to disabling the online-payment button entirely once every year is taken.
- While verifying, Claude caught a real regression in its own first pass: it had dropped the total-fee cap check entirely, reasoning the one-payment-per-year rule made it redundant. But it found Alice Johnson already had $6,000 recorded against a $5,000 fee (a legacy payment plus what looks like a stray test payment I'd made earlier in the session, both with no installmentYear), meaning a student who was already overpaid through the old free-form model could still "pay" for a new year on top under the new rule alone. It reinstated the cap check in both the create and edit routes specifically as a defensive check against pre-existing legacy data, not the primary guard, and confirmed Alice is now correctly blocked while a clean student (Sofia, zero prior payments) can still pay years 1 through 4 normally. Hit the stale-Prisma-Client issue a second time on this same migration and had me restart the dev server again before any of this could be verified.
- I asked for a fresh start to test everything again, so Claude reworked the seed data to actually use installmentYear properly (rather than leaving new seed payments as legacy "Unspecified" rows) and reran the seed script. While doing this it accidentally overwrote the entire seed.ts file with placeholder text via a single Write call meant to be an Edit, caught this immediately from the git diff before doing anything else, and recovered with a plain git checkout rather than trying to reconstruct the file by hand. After reseeding, the dev server errored with ECONNREFUSED, traced to Docker Desktop itself not running rather than any app or schema issue, I started Docker and Claude brought the Postgres container back up and confirmed the reseeded data was reachable.
- I pointed out four issues with the student form at once: enrolment date should default to today for a new student, programme names were getting cut off in the picker, academic year needed a cap tied to degree length (4 for Bachelor's, 2 for Master's), and the fee override field didn't say whether it was annual or total. Claude defaulted enrolmentDate to today only for new students (still editable, unchanged when editing an existing one), widened both Select triggers in this form to w-full (the shadcn Select trigger defaults to w-fit, which was letting the dropdown's width sync to a narrow collapsed state and clip longer names), added a dynamic max on academic year that reacts to the selected programme via watch("programmeId") plus matching help text, and added a line clarifying the fee override replaces the total programme fee, not a per-year amount, still split evenly across installments like the standard fee. The academic year cap was enforced server-side too, in both the create and edit routes, checked against whichever programme applies after the update so it can't be bypassed by changing programme and academic year in the same or separate requests. Verified the server-side cap with curl: a Bachelor's student rejected at year 5, a Master's student rejected at year 3, a valid year 4 Bachelor's student accepted, and an edit attempting to bump an existing student to year 5 also rejected. The dialog's own fields are client-rendered only once opened, so the width/default-date/label changes need a direct browser check rather than curl.
- Mid-task, I said to never commit without asking first, even after verification passes, since I want to test in the browser before anything is locked into history. This reverses how the whole session had been running (auto-committing after each verified change); Claude stopped before committing the student-form fixes above and saved this as a standing instruction for the rest of the project.
- I tested the dialog changes myself and reported the academic year cap wasn't actually working. Claude re-verified the server-side rejection still worked fine with curl, then found the real gap: the HTML max attribute only affects a number input's spinner arrows, not typed values, and the Zod schema has no upper bound (it can't, statically, since the limit depends on which programme is selected). Client-side validation was passing values straight through to the server, which correctly rejected them, but nothing stopped the attempt or explained why in real time. Added a genuine client-side check using the already-watched programmeId and academicYear values, an inline error the moment the typed value exceeds the selected programme's degree length, and the Save button disabled, not just a round-trip rejection.
- I pointed out the dialog had grown too tall. Claude fixed this in the shared DialogContent component itself (max-h-[85vh] overflow-y-auto) rather than one dialog's own className, since none of the app's 5 dialogs had any height cap or scroll behavior at all, a systemic gap that would have hit the next dialog with enough fields too.
- I asked how to delete students, expecting there to be a way since staff can do this in real life. Claude explained the existing README-documented decision (no hard delete, WITHDRAWN represents leaving instead) and asked whether I actually wanted delete added as a real feature. I said yes, specifically for removing wrong entries, not as a replacement for withdrawal. Claude added a DELETE route and a per-row Delete button on the students list, relying on Postgres's existing foreign key constraints (Payment/Submission/Grade all require a Student, no cascade configured) as the actual safeguard rather than writing an application-level check, catching that specific constraint violation (P2003) and turning it into a clear message instead of a generic error. Verified with curl: a freshly created student with no history deletes cleanly (204), Alice Johnson (real payments, submissions, grades) is correctly rejected with a message pointing at Withdrawn instead, and a student role gets a 403 same as every other staff-only mutation.
