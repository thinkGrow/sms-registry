# Copy this file to .env and adjust values if needed.
# Default values below match docker-compose.yml out of the box (`docker compose up -d`).

DATABASE_URL="postgresql://sms:sms@localhost:5432/sms?schema=public"
